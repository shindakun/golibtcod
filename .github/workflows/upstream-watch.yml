name: Upstream watch

# Watches libtcod/libtcod and files triage issues here. Commits touching a C
# file this repo ports get a Claude-triaged issue with a port recommendation
# and a fixture-impact assessment; commits touching modules excluded by design
# roll into one issue. Requires an ANTHROPIC_API_KEY repo secret.
on:
  schedule:
    - cron: "0 13 * * *" # daily ~13:00 UTC
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Print actions instead of creating issues / advancing state"
        type: boolean
        default: false

permissions:
  contents: read
  issues: write

concurrency:
  group: upstream-watch
  cancel-in-progress: false

jobs:
  watch:
    name: Watch libtcod for portable changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run upstream watcher
        env:
          # GH_TOKEN authorizes gh for issues in this repo. The Actions
          # variables API is not writable by github.token, so the SHA-state
          # get/set uses WATCH_PAT (a PAT with Variables:write) when present.
          GH_TOKEN: ${{ github.token }}
          WATCH_PAT: ${{ secrets.WATCH_PAT }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SELF_REPO: ${{ github.repository }}
          DRY_RUN: ${{ (github.event_name == 'workflow_dispatch' && inputs.dry_run) && '1' || '0' }}
        run: bash scripts/upstream-watch.sh
